{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tailwindcss -i ./src/input.css -o ./css/style.css --watch",
    "minify": "npx tailwindcss -i ./src/input.css -o ./css/style.css --minify"
  },
  "dependencies": {
    "bootstrap-icons": "^1.11.3",
    "tailwindcss": "^3.0.15"
  }
}
